-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.profiles (
  id uuid NOT NULL,
  user_id uuid NOT NULL,
  full_name text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  role text NOT NULL DEFAULT 'driver'::text,
  company_id uuid,
  driver_license_number text,
  is_active boolean DEFAULT true,
  email text,
  account_type text DEFAULT 'solo'::text CHECK (account_type = ANY (ARRAY['fleet'::text, 'solo'::text])),
  stripe_customer_id text,
  stripe_subscription_id text,
  subscription_status text DEFAULT 'trial'::text CHECK (subscription_status = ANY (ARRAY['trial'::text, 'active'::text, 'inactive'::text, 'cancelled'::text])),
  subscription_period_end timestamp with time zone,
  trial_ends_at timestamp with time zone,
  payroll_number text,
  phone_number text,
  date_of_birth date,
  national_insurance_number text,
  full_address text,
  emergency_contact_name text,
  emergency_contact_phone text,
  driving_licence_number text,
  driving_licence_expiry date,
  cpc_dqc_number text,
  cpc_dqc_expiry date,
  cpc_training_hours_done integer DEFAULT 0,
  tacho_card_number text,
  tacho_card_expiry date,
  is_contractor boolean DEFAULT false,
  agency_name text,
  last_shift_onboarding_completed_at timestamp with time zone,
  first_time_setup_completed_at timestamp with time zone,
  expo_push_token text,
  deletion_requested_at timestamp with time zone,
  deactivated_at timestamp with time zone,
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT fk_company FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.pay_configurations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  hourly_rate numeric NOT NULL DEFAULT 0,
  overtime_threshold_hours numeric,
  overtime_rate_multiplier numeric DEFAULT 1.5,
  overtime_rate_percentage numeric,
  unpaid_break_minutes numeric NOT NULL DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  additional_overtime_tiers jsonb DEFAULT '[]'::jsonb,
  overtime_threshold_unit text DEFAULT 'day'::text,
  allowance_tiers jsonb DEFAULT '[]'::jsonb,
  CONSTRAINT pay_configurations_pkey PRIMARY KEY (id),
  CONSTRAINT pay_configurations_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.work_sessions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  date date NOT NULL,
  start_time timestamp with time zone NOT NULL,
  end_time timestamp with time zone,
  total_work_minutes integer DEFAULT 0,
  total_poa_minutes integer DEFAULT 0,
  total_break_minutes integer,
  is_manual_entry boolean DEFAULT false,
  timezone text NOT NULL DEFAULT 'UTC'::text,
  created_at timestamp with time zone,
  other_data jsonb,
  compliance_score integer,
  compliance_violations ARRAY,
  status text NOT NULL DEFAULT 'working'::text,
  updated_at timestamp with time zone DEFAULT now(),
  start_lat numeric,
  start_lng numeric,
  end_lat numeric,
  end_lng numeric,
  notes text,
  current_break_start timestamp with time zone,
  current_poa_start timestamp with time zone,
  loaded_miles numeric,
  empty_miles numeric,
  waiting_minutes integer DEFAULT 0,
  job_reference text,
  client_id uuid,
  drop_count integer DEFAULT 1,
  CONSTRAINT work_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT work_sessions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT work_sessions_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.business_profiles (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  user_id uuid NOT NULL UNIQUE,
  created_at timestamp with time zone DEFAULT now(),
  legal_name text,
  address text,
  email text,
  phone text,
  tax_id text,
  bank_account_name text,
  bank_sort_code text,
  bank_account_number text,
  iban text,
  logo_url text,
  vat_number text,
  payment_terms text DEFAULT 'Payment due within 30 days'::text,
  invoice_counter integer DEFAULT 1,
  CONSTRAINT business_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT business_profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.companies (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone DEFAULT now(),
  name text NOT NULL,
  auth_code text UNIQUE,
  auth_code_expires_at timestamp with time zone,
  created_by uuid,
  subscription_status text DEFAULT 'trial'::text,
  max_drivers integer DEFAULT 5,
  stripe_customer_id text,
  stripe_subscription_id text,
  subscription_period_end timestamp with time zone,
  subscription_tier text DEFAULT 'none'::text CHECK (subscription_tier = ANY (ARRAY['none'::text, 'base'::text, 'pro'::text])),
  default_fuel_cost_per_litre numeric,
  pmi_alert_days integer DEFAULT 14,
  CONSTRAINT companies_pkey PRIMARY KEY (id),
  CONSTRAINT fk_created_by FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.invoices (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  driver_id uuid NOT NULL,
  invoice_number text NOT NULL UNIQUE,
  client_name text NOT NULL,
  client_email text,
  client_address text,
  issue_date date NOT NULL DEFAULT CURRENT_DATE,
  due_date date NOT NULL,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'sent'::text, 'paid'::text, 'overdue'::text, 'cancelled'::text])),
  subtotal numeric NOT NULL DEFAULT 0,
  tax_rate numeric DEFAULT 0,
  tax_amount numeric DEFAULT 0,
  total_amount numeric NOT NULL DEFAULT 0,
  currency text NOT NULL DEFAULT 'GBP'::text,
  line_items jsonb DEFAULT '[]'::jsonb,
  notes text,
  payment_terms text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT invoices_pkey PRIMARY KEY (id),
  CONSTRAINT invoices_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.expenses (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  amount numeric NOT NULL,
  category text NOT NULL,
  date date NOT NULL,
  image_url text,
  currency text,
  merchant text,
  notes text,
  raw_ocr_text text,
  fuel_litres numeric,
  session_id uuid,
  vehicle_check_id uuid,
  vehicle_reg text,
  CONSTRAINT expenses_pkey PRIMARY KEY (id),
  CONSTRAINT expenses_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.work_sessions(id),
  CONSTRAINT expenses_vehicle_check_id_fkey FOREIGN KEY (vehicle_check_id) REFERENCES public.vehicle_checks(id),
  CONSTRAINT expenses_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.driver_invites (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  email text NOT NULL,
  full_name text NOT NULL,
  invite_code text NOT NULL UNIQUE,
  status text NOT NULL DEFAULT 'pending'::text,
  pay_config_snapshot jsonb,
  accepted_by_user_id uuid,
  expires_at timestamp with time zone NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  accepted_at timestamp with time zone,
  tacho_card_number text,
  tacho_card_holder_name text,
  tacho_card_expiry date,
  tacho_card_issuing_authority text,
  tacho_source_import_id uuid,
  CONSTRAINT driver_invites_pkey PRIMARY KEY (id),
  CONSTRAINT driver_invites_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT driver_invites_accepted_by_user_id_fkey FOREIGN KEY (accepted_by_user_id) REFERENCES auth.users(id),
  CONSTRAINT driver_invites_tacho_source_import_id_fkey FOREIGN KEY (tacho_source_import_id) REFERENCES public.tachograph_files(id)
);
CREATE TABLE public.driver_documents (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  company_id uuid NOT NULL,
  document_type text NOT NULL,
  storage_path text NOT NULL,
  uploaded_at timestamp with time zone NOT NULL DEFAULT now(),
  uploaded_by uuid,
  id_number text,
  expiry_date date,
  verified_at timestamp with time zone DEFAULT now(),
  CONSTRAINT driver_documents_pkey PRIMARY KEY (id),
  CONSTRAINT driver_documents_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT driver_documents_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT driver_documents_uploaded_by_fkey FOREIGN KEY (uploaded_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.broadcasts (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  company_id uuid NOT NULL,
  sent_by uuid,
  content text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT broadcasts_pkey PRIMARY KEY (id),
  CONSTRAINT broadcasts_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT broadcasts_sent_by_fkey FOREIGN KEY (sent_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.system_messages (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  content text NOT NULL,
  target_audience text NOT NULL DEFAULT 'all_users'::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT system_messages_pkey PRIMARY KEY (id)
);
CREATE TABLE public.vehicle_checks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  driver_id uuid,
  company_id uuid,
  reg_number text NOT NULL,
  vehicle_type text NOT NULL,
  vehicle_make text,
  check_status text DEFAULT 'pass'::text,
  items jsonb NOT NULL,
  defect_details text,
  created_at timestamp with time zone DEFAULT now(),
  session_id uuid,
  odometer_reading integer,
  trailer_reg text,
  defect_lifecycle_status text DEFAULT 'reported'::text CHECK (defect_lifecycle_status = ANY (ARRAY['reported'::text, 'in_progress'::text, 'fixed'::text])),
  resolution_notes text,
  resolved_at timestamp with time zone,
  resolved_by uuid,
  closing_odometer integer,
  inspection_duration_seconds integer,
  signature_url text,
  CONSTRAINT vehicle_checks_pkey PRIMARY KEY (id),
  CONSTRAINT vehicle_checks_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT vehicle_checks_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT vehicle_checks_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.work_sessions(id),
  CONSTRAINT vehicle_checks_resolved_by_fkey FOREIGN KEY (resolved_by) REFERENCES auth.users(id)
);
CREATE TABLE public.vehicles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid,
  reg_number text NOT NULL UNIQUE,
  make text NOT NULL,
  model text,
  year integer,
  vehicle_type text NOT NULL,
  vin_number text,
  is_vor boolean DEFAULT false,
  status_notes text,
  current_odometer integer DEFAULT 0,
  mot_due_date date,
  pmi_due_date date,
  tacho_calibration_due date,
  loler_due_date date,
  insurance_expiry date,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  pmi_interval_weeks integer DEFAULT 6,
  maintenance_called boolean DEFAULT false,
  user_id uuid UNIQUE,
  vehicle_class text DEFAULT 'rigid'::text CHECK (vehicle_class = ANY (ARRAY['rigid'::text, 'artic'::text, 'trailer'::text, 'van'::text])),
  CONSTRAINT vehicles_pkey PRIMARY KEY (id),
  CONSTRAINT vehicles_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT vehicles_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.maintenance_logs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  vehicle_id uuid,
  company_id uuid,
  event_type text NOT NULL,
  service_provider text,
  odometer_at_service integer,
  cost numeric,
  description text,
  document_url text,
  completed_at date NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  created_by uuid,
  CONSTRAINT maintenance_logs_pkey PRIMARY KEY (id),
  CONSTRAINT maintenance_logs_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT maintenance_logs_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT maintenance_logs_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.message_reads (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  message_id uuid,
  read_at timestamp with time zone DEFAULT now(),
  CONSTRAINT message_reads_pkey PRIMARY KEY (id),
  CONSTRAINT message_reads_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.tacho_activities (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  driver_id uuid,
  company_id uuid,
  activity_type text NOT NULL,
  start_time timestamp with time zone NOT NULL,
  end_time timestamp with time zone NOT NULL,
  duration_minutes integer,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT tacho_activities_pkey PRIMARY KEY (id),
  CONSTRAINT tacho_activities_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT tacho_activities_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id)
);
CREATE TABLE public.tacho_infringements (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  driver_id uuid,
  company_id uuid,
  occurrence_date date NOT NULL,
  infringement_type text NOT NULL,
  description text,
  driver_explanation text,
  is_signed boolean DEFAULT false,
  signed_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT tacho_infringements_pkey PRIMARY KEY (id),
  CONSTRAINT tacho_infringements_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT tacho_infringements_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id)
);
CREATE TABLE public.clients (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  name text NOT NULL,
  address text,
  email text,
  created_at timestamp with time zone DEFAULT now(),
  billing_type text DEFAULT 'hourly'::text,
  hourly_rate numeric,
  daily_rate numeric,
  night_out_rate numeric,
  ppm_loaded_rate numeric,
  ppm_empty_rate numeric,
  waiting_time_rate numeric,
  waiting_time_free_minutes integer DEFAULT 60,
  fuel_surcharge_pct numeric DEFAULT 0,
  payment_terms text DEFAULT 'Payment due within 30 days'::text,
  notes text,
  custom_line_items jsonb DEFAULT '[]'::jsonb,
  billing_types text,
  CONSTRAINT clients_pkey PRIMARY KEY (id),
  CONSTRAINT clients_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.billing_rates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  rate_type text NOT NULL,
  base_rate numeric NOT NULL,
  overtime_threshold_hours numeric,
  overtime_multiplier numeric DEFAULT 1.5,
  empty_running_rate numeric,
  fuel_surcharge_pct numeric DEFAULT 0,
  waiting_time_rate numeric,
  waiting_time_free_minutes integer DEFAULT 60,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT billing_rates_pkey PRIMARY KEY (id),
  CONSTRAINT billing_rates_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.vehicle_documents (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  vehicle_id uuid,
  company_id uuid,
  document_type text NOT NULL,
  storage_path text NOT NULL,
  id_number text,
  expiry_date date,
  uploaded_at timestamp with time zone DEFAULT now(),
  uploaded_by uuid,
  CONSTRAINT vehicle_documents_pkey PRIMARY KEY (id),
  CONSTRAINT vehicle_documents_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT vehicle_documents_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT vehicle_documents_uploaded_by_fkey FOREIGN KEY (uploaded_by) REFERENCES auth.users(id)
);
CREATE TABLE public.shift_jobs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  session_id uuid,
  user_id uuid,
  client_id uuid,
  job_reference text,
  collection_point text,
  delivery_point text,
  loaded_miles numeric DEFAULT 0,
  empty_miles numeric DEFAULT 0,
  waiting_minutes integer DEFAULT 0,
  night_out boolean DEFAULT false,
  drop_count integer DEFAULT 1,
  notes text,
  logged_at timestamp with time zone DEFAULT now(),
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT shift_jobs_pkey PRIMARY KEY (id),
  CONSTRAINT shift_jobs_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.work_sessions(id),
  CONSTRAINT shift_jobs_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT shift_jobs_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.training_records (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid,
  driver_id uuid,
  training_type text NOT NULL,
  module_id text,
  title text NOT NULL,
  hours_credited numeric DEFAULT 0,
  status text DEFAULT 'assigned'::text,
  assigned_by uuid,
  assigned_at timestamp with time zone DEFAULT now(),
  completed_at timestamp with time zone,
  notes text,
  CONSTRAINT training_records_pkey PRIMARY KEY (id),
  CONSTRAINT training_records_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT training_records_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT training_records_assigned_by_fkey FOREIGN KEY (assigned_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.defect_photos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  vehicle_check_id uuid,
  storage_path text NOT NULL,
  uploaded_at timestamp with time zone DEFAULT now(),
  CONSTRAINT defect_photos_pkey PRIMARY KEY (id),
  CONSTRAINT defect_photos_vehicle_check_id_fkey FOREIGN KEY (vehicle_check_id) REFERENCES public.vehicle_checks(id)
);
CREATE TABLE public.messages (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  sender_id uuid,
  recipient_id uuid,
  body text NOT NULL,
  read_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT messages_pkey PRIMARY KEY (id),
  CONSTRAINT messages_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT messages_sender_id_fkey FOREIGN KEY (sender_id) REFERENCES auth.users(id),
  CONSTRAINT messages_recipient_id_fkey FOREIGN KEY (recipient_id) REFERENCES auth.users(id)
);
CREATE TABLE public.infringements (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  driver_id uuid NOT NULL,
  session_id uuid,
  violation_type text NOT NULL,
  regulation text NOT NULL DEFAULT 'REG_561'::text CHECK (regulation = ANY (ARRAY['REG_561'::text, 'WTD'::text, 'DOMESTIC'::text, 'OTHER'::text])),
  severity text NOT NULL DEFAULT 'minor'::text CHECK (severity = ANY (ARRAY['minor'::text, 'serious'::text, 'very_serious'::text])),
  occurred_at date NOT NULL,
  status text NOT NULL DEFAULT 'open'::text CHECK (status = ANY (ARRAY['open'::text, 'debriefed'::text, 'closed'::text])),
  driver_statement text,
  manager_notes text,
  debriefed_at timestamp with time zone,
  debriefed_by uuid,
  training_record_id uuid,
  created_at timestamp with time zone DEFAULT now(),
  created_by uuid,
  CONSTRAINT infringements_pkey PRIMARY KEY (id),
  CONSTRAINT infringements_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT infringements_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT infringements_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.work_sessions(id),
  CONSTRAINT infringements_debriefed_by_fkey FOREIGN KEY (debriefed_by) REFERENCES auth.users(id),
  CONSTRAINT infringements_training_record_id_fkey FOREIGN KEY (training_record_id) REFERENCES public.training_records(id),
  CONSTRAINT infringements_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.fuel_logs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid,
  reg_number text NOT NULL,
  vehicle_id uuid,
  driver_id uuid NOT NULL,
  vehicle_check_id uuid,
  log_date date NOT NULL DEFAULT CURRENT_DATE,
  start_odometer numeric,
  end_odometer numeric,
  distance_covered numeric DEFAULT 
CASE
    WHEN ((end_odometer IS NOT NULL) AND (start_odometer IS NOT NULL)) THEN (end_odometer - start_odometer)
    ELSE NULL::numeric
END,
  fuel_added_litres numeric NOT NULL DEFAULT 0,
  fuel_type text NOT NULL CHECK (fuel_type = ANY (ARRAY['diesel'::text, 'petrol'::text, 'hvo'::text, 'adblue'::text, 'electric_kwh'::text, 'other'::text])),
  fuel_cost_per_litre numeric,
  total_fuel_cost numeric,
  mpg numeric,
  notes text,
  source text NOT NULL DEFAULT 'driver_app'::text CHECK (source = ANY (ARRAY['driver_app'::text, 'manual_entry'::text])),
  created_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT fuel_logs_pkey PRIMARY KEY (id),
  CONSTRAINT fuel_logs_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT fuel_logs_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT fuel_logs_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES auth.users(id),
  CONSTRAINT fuel_logs_vehicle_check_id_fkey FOREIGN KEY (vehicle_check_id) REFERENCES public.vehicle_checks(id),
  CONSTRAINT fuel_logs_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.shifts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  driver_id uuid NOT NULL,
  vehicle_id uuid,
  date date NOT NULL,
  start_time time without time zone NOT NULL,
  end_time time without time zone NOT NULL,
  notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT shifts_pkey PRIMARY KEY (id),
  CONSTRAINT shifts_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT shifts_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT shifts_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id)
);
CREATE TABLE public.incidents (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  driver_id uuid NOT NULL,
  vehicle_id uuid,
  type text NOT NULL,
  occurred_at timestamp with time zone NOT NULL,
  location text NOT NULL,
  description text NOT NULL,
  has_injury boolean DEFAULT false,
  injury_details text,
  is_third_party_involved boolean DEFAULT false,
  third_party_details jsonb DEFAULT '{}'::jsonb,
  police_ref text,
  photo_urls ARRAY DEFAULT '{}'::text[],
  status text NOT NULL DEFAULT 'reported'::text,
  manager_notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT incidents_pkey PRIMARY KEY (id),
  CONSTRAINT incidents_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT incidents_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT incidents_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id)
);
CREATE TABLE public.tachograph_files (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  company_id uuid,
  driver_id uuid,
  filename text NOT NULL,
  file_path text NOT NULL,
  file_type text,
  status text DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'processing'::text, 'processed'::text, 'partial'::text, 'error'::text])),
  uploaded_at timestamp with time zone DEFAULT now(),
  metadata jsonb,
  processed_at timestamp with time zone,
  external_card_number text,
  vehicle_id uuid,
  source_type text DEFAULT 'driver_card'::text,
  CONSTRAINT tachograph_files_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_files_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT tachograph_files_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT tachograph_files_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id)
);
CREATE TABLE public.tachograph_activities (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  file_id uuid,
  driver_id uuid,
  start_time timestamp with time zone NOT NULL,
  end_time timestamp with time zone NOT NULL,
  activity_type text NOT NULL CHECK (activity_type = ANY (ARRAY['driving'::text, 'work'::text, 'poa'::text, 'rest'::text])),
  slot integer DEFAULT 1,
  is_manual_entry boolean DEFAULT false,
  distance_km numeric,
  CONSTRAINT tachograph_activities_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_activities_file_id_fkey FOREIGN KEY (file_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT tachograph_activities_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.tachograph_speed_logs (
  id bigint NOT NULL DEFAULT nextval('tachograph_speed_logs_id_seq'::regclass),
  file_id uuid,
  timestamp timestamp with time zone NOT NULL,
  speed_kmh integer NOT NULL,
  CONSTRAINT tachograph_speed_logs_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_speed_logs_file_id_fkey FOREIGN KEY (file_id) REFERENCES public.tachograph_files(id)
);
CREATE TABLE public.alerts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  type text NOT NULL,
  severity text NOT NULL DEFAULT 'warning'::text,
  message text NOT NULL,
  metadata jsonb DEFAULT '{}'::jsonb,
  is_read boolean NOT NULL DEFAULT false,
  is_dismissed boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT alerts_pkey PRIMARY KEY (id),
  CONSTRAINT alerts_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id)
);
CREATE TABLE public.tachograph_processing_runs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  import_id uuid NOT NULL,
  company_id uuid,
  parser_version text NOT NULL,
  source text NOT NULL DEFAULT 'normalized_findings'::text,
  warnings jsonb NOT NULL DEFAULT '[]'::jsonb,
  errors jsonb NOT NULL DEFAULT '[]'::jsonb,
  processed_at timestamp with time zone NOT NULL DEFAULT now(),
  parser_name text NOT NULL,
  parser_config_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  status text NOT NULL DEFAULT 'completed'::text CHECK (status = ANY (ARRAY['queued'::text, 'running'::text, 'completed'::text, 'completed_with_warnings'::text, 'failed'::text, 'unsupported'::text, 'superseded'::text])),
  started_at timestamp with time zone,
  completed_at timestamp with time zone,
  duration_ms integer CHECK (duration_ms IS NULL OR duration_ms >= 0),
  error_summary text,
  triggered_by text,
  supersedes_parser_run_id uuid,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  is_current boolean NOT NULL DEFAULT true,
  run_sequence integer,
  CONSTRAINT tachograph_processing_runs_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_processing_runs_supersedes_parser_run_id_fkey FOREIGN KEY (supersedes_parser_run_id) REFERENCES public.tachograph_processing_runs(id),
  CONSTRAINT tachograph_processing_runs_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT tachograph_processing_runs_import_id_fkey FOREIGN KEY (import_id) REFERENCES public.tachograph_files(id)
);
CREATE TABLE public.driver_card_downloads (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  import_id uuid NOT NULL,
  company_id uuid,
  driver_id uuid,
  driver_name text,
  card_number text NOT NULL,
  card_expiry date,
  issuing_country text,
  downloaded_at timestamp with time zone NOT NULL,
  period_start timestamp with time zone NOT NULL,
  period_end timestamp with time zone NOT NULL,
  download_status text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  parser_run_id uuid,
  CONSTRAINT driver_card_downloads_pkey PRIMARY KEY (id),
  CONSTRAINT driver_card_downloads_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id),
  CONSTRAINT driver_card_downloads_import_id_fkey FOREIGN KEY (import_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT driver_card_downloads_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT driver_card_downloads_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.vehicle_unit_downloads (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  import_id uuid NOT NULL,
  company_id uuid,
  vehicle_id uuid,
  reg_number text,
  vu_serial text NOT NULL,
  calibration_due date,
  downloaded_at timestamp with time zone NOT NULL,
  period_start timestamp with time zone NOT NULL,
  period_end timestamp with time zone NOT NULL,
  download_status text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  parser_run_id uuid,
  CONSTRAINT vehicle_unit_downloads_pkey PRIMARY KEY (id),
  CONSTRAINT vehicle_unit_downloads_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id),
  CONSTRAINT vehicle_unit_downloads_import_id_fkey FOREIGN KEY (import_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT vehicle_unit_downloads_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT vehicle_unit_downloads_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id)
);
CREATE TABLE public.tachograph_activity_segments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  import_id uuid NOT NULL,
  company_id uuid,
  driver_id uuid,
  vehicle_id uuid,
  source text NOT NULL,
  activity_type text NOT NULL,
  start_time timestamp with time zone NOT NULL,
  end_time timestamp with time zone NOT NULL,
  duration_mins integer NOT NULL,
  distance_km numeric,
  confidence text,
  label text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  parser_run_id uuid,
  CONSTRAINT tachograph_activity_segments_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_activity_segments_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id),
  CONSTRAINT tachograph_activity_segments_import_id_fkey FOREIGN KEY (import_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT tachograph_activity_segments_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT tachograph_activity_segments_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT tachograph_activity_segments_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id)
);
CREATE TABLE public.tachograph_day_summaries (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  import_id uuid NOT NULL,
  company_id uuid,
  driver_id uuid,
  vehicle_id uuid,
  summary_date date NOT NULL,
  driving_mins integer NOT NULL DEFAULT 0,
  work_mins integer NOT NULL DEFAULT 0,
  poa_mins integer NOT NULL DEFAULT 0,
  rest_mins integer NOT NULL DEFAULT 0,
  app_driving_mins integer,
  findings_count integer NOT NULL DEFAULT 0,
  vu_event_count integer,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  parser_run_id uuid,
  CONSTRAINT tachograph_day_summaries_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_day_summaries_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id),
  CONSTRAINT tachograph_day_summaries_import_id_fkey FOREIGN KEY (import_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT tachograph_day_summaries_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT tachograph_day_summaries_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT tachograph_day_summaries_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id)
);
CREATE TABLE public.tachograph_findings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  import_id uuid NOT NULL,
  company_id uuid,
  driver_id uuid,
  vehicle_id uuid,
  source text NOT NULL,
  severity text NOT NULL,
  status text NOT NULL,
  rule_code text NOT NULL,
  title text NOT NULL,
  summary text NOT NULL,
  legal_basis text,
  occurred_at timestamp with time zone NOT NULL,
  period_start timestamp with time zone NOT NULL,
  period_end timestamp with time zone NOT NULL,
  evidence_refs jsonb NOT NULL DEFAULT '[]'::jsonb,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  parser_run_id uuid,
  CONSTRAINT tachograph_findings_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_findings_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id),
  CONSTRAINT tachograph_findings_import_id_fkey FOREIGN KEY (import_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT tachograph_findings_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT tachograph_findings_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT tachograph_findings_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id)
);
CREATE TABLE public.tachograph_technical_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  import_id uuid NOT NULL,
  company_id uuid,
  driver_id uuid,
  vehicle_id uuid,
  source text NOT NULL DEFAULT 'vehicle_unit'::text,
  severity text NOT NULL,
  status text NOT NULL,
  rule_code text NOT NULL,
  title text NOT NULL,
  summary text NOT NULL,
  occurred_at timestamp with time zone NOT NULL,
  period_start timestamp with time zone NOT NULL,
  period_end timestamp with time zone NOT NULL,
  evidence_refs jsonb NOT NULL DEFAULT '[]'::jsonb,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  parser_run_id uuid,
  CONSTRAINT tachograph_technical_events_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_technical_events_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id),
  CONSTRAINT tachograph_technical_events_import_id_fkey FOREIGN KEY (import_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT tachograph_technical_events_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT tachograph_technical_events_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT tachograph_technical_events_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id)
);
CREATE TABLE public.driver_tacho_compliance_signals (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid,
  driver_id uuid NOT NULL,
  period_days integer NOT NULL,
  average_score integer NOT NULL,
  total_violations integer NOT NULL,
  violations jsonb NOT NULL DEFAULT '[]'::jsonb,
  recent_violations jsonb NOT NULL DEFAULT '[]'::jsonb,
  missing_mileage jsonb NOT NULL DEFAULT '[]'::jsonb,
  has_data boolean NOT NULL DEFAULT false,
  source text NOT NULL DEFAULT 'normalized_findings'::text,
  generated_at timestamp with time zone NOT NULL DEFAULT now(),
  reconciliation_summary jsonb NOT NULL DEFAULT '{"appOnlyDays": 0, "matchedDays": 0, "totalIssues": 0, "tachoOnlyDays": 0, "uncertainDays": 0, "mismatchActivityDays": 0, "mismatchDurationDays": 0}'::jsonb,
  review_focus jsonb,
  CONSTRAINT driver_tacho_compliance_signals_pkey PRIMARY KEY (id),
  CONSTRAINT driver_tacho_compliance_signals_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT driver_tacho_compliance_signals_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.driver_tacho_risk_signals (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid,
  driver_id uuid NOT NULL,
  period_days integer NOT NULL,
  legal_compliance_score integer NOT NULL,
  violation_count integer NOT NULL,
  missing_mileage_count integer NOT NULL DEFAULT 0,
  app_mismatch_count integer NOT NULL DEFAULT 0,
  source text NOT NULL DEFAULT 'normalized_findings'::text,
  generated_at timestamp with time zone NOT NULL DEFAULT now(),
  reconciliation_summary jsonb NOT NULL DEFAULT '{"appOnlyDays": 0, "matchedDays": 0, "totalIssues": 0, "tachoOnlyDays": 0, "uncertainDays": 0, "mismatchActivityDays": 0, "mismatchDurationDays": 0}'::jsonb,
  review_focus jsonb,
  CONSTRAINT driver_tacho_risk_signals_pkey PRIMARY KEY (id),
  CONSTRAINT driver_tacho_risk_signals_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT driver_tacho_risk_signals_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.tachograph_vehicle_motion_discrepancies (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  import_id uuid NOT NULL,
  company_id uuid,
  driver_id uuid,
  vehicle_id uuid,
  discrepancy_date date NOT NULL,
  start_time timestamp with time zone NOT NULL,
  end_time timestamp with time zone NOT NULL,
  duration_mins integer NOT NULL DEFAULT 0,
  severity text NOT NULL,
  status text NOT NULL,
  summary text NOT NULL,
  linked_driver_name text,
  evidence_refs jsonb NOT NULL DEFAULT '[]'::jsonb,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  parser_run_id uuid,
  CONSTRAINT tachograph_vehicle_motion_discrepancies_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_vehicle_motion_discrepancies_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id),
  CONSTRAINT tachograph_vehicle_motion_discrepancies_import_id_fkey FOREIGN KEY (import_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT tachograph_vehicle_motion_discrepancies_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT tachograph_vehicle_motion_discrepancies_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT tachograph_vehicle_motion_discrepancies_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id)
);
CREATE TABLE public.tachograph_reconciliation_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  import_id uuid NOT NULL,
  company_id uuid,
  driver_id uuid,
  vehicle_id uuid,
  recon_date date NOT NULL,
  status text NOT NULL,
  app_label text NOT NULL,
  tacho_label text NOT NULL,
  summary text NOT NULL,
  app_driving_mins integer NOT NULL DEFAULT 0,
  tacho_driving_mins integer NOT NULL DEFAULT 0,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  parser_run_id uuid,
  CONSTRAINT tachograph_reconciliation_items_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_reconciliation_items_import_id_fkey FOREIGN KEY (import_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT tachograph_reconciliation_items_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT tachograph_reconciliation_items_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT tachograph_reconciliation_items_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT tachograph_reconciliation_items_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id)
);
CREATE TABLE public.work_session_segments (
  id text NOT NULL,
  session_id uuid NOT NULL,
  user_id uuid NOT NULL,
  activity_type text NOT NULL CHECK (activity_type = ANY (ARRAY['work'::text, 'break'::text, 'poa'::text, 'driving_reference'::text])),
  start_time timestamp with time zone NOT NULL,
  end_time timestamp with time zone,
  source text NOT NULL DEFAULT 'manual'::text,
  confidence numeric NOT NULL DEFAULT 1 CHECK (confidence >= 0::numeric AND confidence <= 1::numeric),
  client_created_at timestamp with time zone NOT NULL,
  client_updated_at timestamp with time zone NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT work_session_segments_pkey PRIMARY KEY (id),
  CONSTRAINT work_session_segments_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.work_sessions(id),
  CONSTRAINT work_session_segments_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.tachograph_finding_reviews (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  finding_id uuid NOT NULL UNIQUE,
  company_id uuid NOT NULL,
  driver_id uuid,
  import_id uuid NOT NULL,
  status text NOT NULL DEFAULT 'open'::text CHECK (status = ANY (ARRAY['open'::text, 'reviewed'::text, 'action_required'::text, 'closed'::text])),
  manager_note text,
  corrective_action_type text CHECK (corrective_action_type IS NULL OR (corrective_action_type = ANY (ARRAY['training'::text, 'manager_debrief'::text, 'manual_entry'::text, 'other'::text]))),
  corrective_action_ref_id uuid,
  reviewed_at timestamp with time zone,
  reviewed_by_user_id uuid,
  closed_at timestamp with time zone,
  closed_by_user_id uuid,
  driver_acknowledged_at timestamp with time zone,
  driver_acknowledged_by_user_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tachograph_finding_reviews_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_finding_reviews_finding_id_fkey FOREIGN KEY (finding_id) REFERENCES public.tachograph_findings(id),
  CONSTRAINT tachograph_finding_reviews_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT tachograph_finding_reviews_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT tachograph_finding_reviews_import_id_fkey FOREIGN KEY (import_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT tachograph_finding_reviews_reviewed_by_user_id_fkey FOREIGN KEY (reviewed_by_user_id) REFERENCES public.profiles(id),
  CONSTRAINT tachograph_finding_reviews_closed_by_user_id_fkey FOREIGN KEY (closed_by_user_id) REFERENCES public.profiles(id),
  CONSTRAINT tachograph_finding_reviews_driver_acknowledged_by_user_id_fkey FOREIGN KEY (driver_acknowledged_by_user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.tachograph_finding_review_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  review_id uuid NOT NULL,
  finding_id uuid NOT NULL,
  company_id uuid NOT NULL,
  actor_user_id uuid,
  event_type text NOT NULL CHECK (event_type = ANY (ARRAY['created'::text, 'updated'::text, 'driver_acknowledged'::text])),
  previous_status text,
  new_status text NOT NULL,
  note text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tachograph_finding_review_events_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_finding_review_events_review_id_fkey FOREIGN KEY (review_id) REFERENCES public.tachograph_finding_reviews(id),
  CONSTRAINT tachograph_finding_review_events_finding_id_fkey FOREIGN KEY (finding_id) REFERENCES public.tachograph_findings(id),
  CONSTRAINT tachograph_finding_review_events_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT tachograph_finding_review_events_actor_user_id_fkey FOREIGN KEY (actor_user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.tachograph_parser_outputs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parser_run_id uuid NOT NULL,
  import_id uuid NOT NULL,
  company_id uuid,
  output_type text NOT NULL,
  payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tachograph_parser_outputs_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_parser_outputs_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id),
  CONSTRAINT tachograph_parser_outputs_import_id_fkey FOREIGN KEY (import_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT tachograph_parser_outputs_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id)
);
CREATE TABLE public.tachograph_parser_errors (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parser_run_id uuid NOT NULL,
  import_id uuid NOT NULL,
  company_id uuid,
  severity text NOT NULL DEFAULT 'error'::text CHECK (severity = ANY (ARRAY['info'::text, 'warning'::text, 'error'::text, 'critical'::text])),
  error_code text NOT NULL DEFAULT 'parser_error'::text,
  message text NOT NULL,
  details_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tachograph_parser_errors_pkey PRIMARY KEY (id),
  CONSTRAINT tachograph_parser_errors_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id),
  CONSTRAINT tachograph_parser_errors_import_id_fkey FOREIGN KEY (import_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT tachograph_parser_errors_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id)
);
CREATE TABLE public.timeline_generations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  driver_id uuid,
  vehicle_id uuid,
  scope_type text NOT NULL CHECK (scope_type = ANY (ARRAY['driver'::text, 'vehicle'::text, 'import'::text, 'fleet_range'::text])),
  scope_id uuid,
  range_start timestamp with time zone NOT NULL,
  range_end timestamp with time zone NOT NULL,
  generation_version text NOT NULL DEFAULT 'timeline-mvp@1'::text,
  status text NOT NULL DEFAULT 'running'::text CHECK (status = ANY (ARRAY['running'::text, 'completed'::text, 'completed_with_warnings'::text, 'failed'::text, 'superseded'::text])),
  is_current boolean NOT NULL DEFAULT true,
  generated_by uuid,
  generated_by_kind text NOT NULL DEFAULT 'system'::text CHECK (generated_by_kind = ANY (ARRAY['system'::text, 'manager'::text, 'driver'::text, 'trigger'::text, 'backfill'::text])),
  generated_reason text NOT NULL DEFAULT 'initial_import'::text CHECK (generated_reason = ANY (ARRAY['initial_import'::text, 'reprocess'::text, 'pairing_changed'::text, 'manual_rebuild'::text, 'backfill'::text])),
  source_import_id uuid,
  parser_run_id uuid,
  supersedes_generation_id uuid,
  superseded_by_generation_id uuid,
  superseded_at timestamp with time zone,
  started_at timestamp with time zone NOT NULL DEFAULT now(),
  completed_at timestamp with time zone,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT timeline_generations_pkey PRIMARY KEY (id),
  CONSTRAINT timeline_generations_supersedes_generation_id_fkey FOREIGN KEY (supersedes_generation_id) REFERENCES public.timeline_generations(id),
  CONSTRAINT timeline_generations_superseded_by_generation_id_fkey FOREIGN KEY (superseded_by_generation_id) REFERENCES public.timeline_generations(id),
  CONSTRAINT timeline_generations_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT timeline_generations_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT timeline_generations_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT timeline_generations_generated_by_fkey FOREIGN KEY (generated_by) REFERENCES public.profiles(id),
  CONSTRAINT timeline_generations_source_import_id_fkey FOREIGN KEY (source_import_id) REFERENCES public.tachograph_files(id),
  CONSTRAINT timeline_generations_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id)
);
CREATE TABLE public.timeline_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  timeline_generation_id uuid NOT NULL,
  driver_id uuid,
  vehicle_id uuid,
  event_type text NOT NULL CHECK (length(btrim(event_type)) > 0),
  started_at timestamp with time zone NOT NULL,
  ended_at timestamp with time zone,
  duration_seconds integer CHECK (duration_seconds IS NULL OR duration_seconds >= 0),
  timezone text NOT NULL DEFAULT 'UTC'::text,
  confidence_state text NOT NULL DEFAULT 'uncertain'::text CHECK (confidence_state = ANY (ARRAY['confirmed'::text, 'likely'::text, 'possible'::text, 'uncertain'::text, 'insufficient_data'::text])),
  source_summary text NOT NULL DEFAULT 'Timeline generation'::text,
  status text NOT NULL DEFAULT 'current'::text CHECK (status = ANY (ARRAY['current'::text, 'superseded'::text, 'excluded'::text, 'needs_review'::text])),
  is_current boolean NOT NULL DEFAULT true,
  parser_run_id uuid,
  import_file_id uuid,
  source_table text,
  source_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT timeline_events_pkey PRIMARY KEY (id),
  CONSTRAINT timeline_events_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT timeline_events_timeline_generation_id_fkey FOREIGN KEY (timeline_generation_id) REFERENCES public.timeline_generations(id),
  CONSTRAINT timeline_events_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT timeline_events_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT timeline_events_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id),
  CONSTRAINT timeline_events_import_file_id_fkey FOREIGN KEY (import_file_id) REFERENCES public.tachograph_files(id)
);
CREATE TABLE public.timeline_event_sources (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  timeline_event_id uuid NOT NULL,
  timeline_generation_id uuid NOT NULL,
  source_type text NOT NULL CHECK (source_type = ANY (ARRAY['tachograph_activity_segment'::text, 'tachograph_technical_event'::text, 'vehicle_motion_discrepancy'::text, 'tachograph_reconciliation_item'::text, 'work_session'::text, 'driver_card_import'::text, 'vehicle_unit_import'::text, 'manual_entry'::text, 'future_telematics'::text])),
  source_id uuid,
  source_external_id text,
  normalised_activity_id uuid,
  parser_run_id uuid,
  parser_output_id uuid,
  import_file_id uuid,
  source_reference_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT timeline_event_sources_pkey PRIMARY KEY (id),
  CONSTRAINT timeline_event_sources_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT timeline_event_sources_timeline_event_id_fkey FOREIGN KEY (timeline_event_id) REFERENCES public.timeline_events(id),
  CONSTRAINT timeline_event_sources_timeline_generation_id_fkey FOREIGN KEY (timeline_generation_id) REFERENCES public.timeline_generations(id),
  CONSTRAINT timeline_event_sources_parser_run_id_fkey FOREIGN KEY (parser_run_id) REFERENCES public.tachograph_processing_runs(id),
  CONSTRAINT timeline_event_sources_parser_output_id_fkey FOREIGN KEY (parser_output_id) REFERENCES public.tachograph_parser_outputs(id),
  CONSTRAINT timeline_event_sources_import_file_id_fkey FOREIGN KEY (import_file_id) REFERENCES public.tachograph_files(id)
);
CREATE TABLE public.timeline_gaps (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  timeline_generation_id uuid NOT NULL,
  driver_id uuid,
  vehicle_id uuid,
  started_at timestamp with time zone NOT NULL,
  ended_at timestamp with time zone NOT NULL,
  duration_seconds integer NOT NULL CHECK (duration_seconds >= 0),
  gap_type text NOT NULL CHECK (gap_type = ANY (ARRAY['missing_driver_card_data'::text, 'missing_vehicle_unit_data'::text, 'unmatched_activity'::text, 'unknown_driver'::text, 'parser_gap'::text, 'manual_review_required'::text, 'app_tacho_mismatch'::text])),
  severity text NOT NULL DEFAULT 'medium'::text CHECK (severity = ANY (ARRAY['low'::text, 'medium'::text, 'high'::text, 'critical'::text])),
  reason text NOT NULL,
  status text NOT NULL DEFAULT 'open'::text CHECK (status = ANY (ARRAY['open'::text, 'in_review'::text, 'resolved'::text, 'superseded'::text, 'accepted'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT timeline_gaps_pkey PRIMARY KEY (id),
  CONSTRAINT timeline_gaps_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT timeline_gaps_timeline_generation_id_fkey FOREIGN KEY (timeline_generation_id) REFERENCES public.timeline_generations(id),
  CONSTRAINT timeline_gaps_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT timeline_gaps_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id)
);
CREATE TABLE public.daily_timeline_summaries (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  timeline_generation_id uuid NOT NULL,
  driver_id uuid,
  vehicle_id uuid,
  summary_date date NOT NULL,
  driving_seconds integer NOT NULL DEFAULT 0,
  work_seconds integer NOT NULL DEFAULT 0,
  availability_seconds integer NOT NULL DEFAULT 0,
  rest_seconds integer NOT NULL DEFAULT 0,
  break_seconds integer NOT NULL DEFAULT 0,
  unknown_seconds integer NOT NULL DEFAULT 0,
  duty_start timestamp with time zone,
  duty_end timestamp with time zone,
  gap_count integer NOT NULL DEFAULT 0,
  finding_count integer NOT NULL DEFAULT 0,
  confidence_state text NOT NULL DEFAULT 'uncertain'::text CHECK (confidence_state = ANY (ARRAY['confirmed'::text, 'likely'::text, 'possible'::text, 'uncertain'::text, 'insufficient_data'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT daily_timeline_summaries_pkey PRIMARY KEY (id),
  CONSTRAINT daily_timeline_summaries_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT daily_timeline_summaries_timeline_generation_id_fkey FOREIGN KEY (timeline_generation_id) REFERENCES public.timeline_generations(id),
  CONSTRAINT daily_timeline_summaries_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.profiles(id),
  CONSTRAINT daily_timeline_summaries_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id)
);
CREATE TABLE public.account_deletion_requests (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  profile_id uuid,
  email text,
  company_id uuid,
  role text,
  requested_at timestamp with time zone NOT NULL DEFAULT now(),
  status text NOT NULL DEFAULT 'requested'::text CHECK (status = ANY (ARRAY['requested'::text, 'reviewing'::text, 'completed'::text, 'rejected'::text, 'cancelled'::text])),
  reason text,
  source text NOT NULL DEFAULT 'mobile_app'::text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT account_deletion_requests_pkey PRIMARY KEY (id),
  CONSTRAINT account_deletion_requests_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT account_deletion_requests_profile_id_fkey FOREIGN KEY (profile_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.public_atlas_feedback (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  name text,
  email text,
  message_type text NOT NULL CHECK (message_type = ANY (ARRAY['question'::text, 'feature_request'::text, 'unknown_query'::text])),
  message_content text NOT NULL,
  atlas_matched_intent text,
  user_agent text,
  consent_contact boolean NOT NULL DEFAULT false,
  CONSTRAINT public_atlas_feedback_pkey PRIMARY KEY (id)
);