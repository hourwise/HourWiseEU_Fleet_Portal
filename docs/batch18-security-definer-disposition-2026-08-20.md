# Batch 18 SECURITY DEFINER disposition

Date: 2026-08-20  
Repository: `hourwise/HourWiseEU_Fleet_Portal`  
Starting commit: `11ebb0edaf2ffb5c193e7564154a24399bf5b410`  
Database project: `lcvahjmoobmpifrexurb`

## Decision record

This is an owner-reviewable disposition of the remaining SECURITY DEFINER surface, not a blanket instruction to remove it. The Batch 17 live advisor baseline was 73 authenticated execution notices, 3 intentional anonymous onboarding notices, 16 RLS-enabled internal tables without browser policies, and one Auth password-protection setting. Mutable search-path findings were zero.

For every retained browser RPC, the review evidence is the repository caller, `auth.uid()` actor derivation, `get_my_company_id()` company scope, current role/permission check, target ownership check where applicable, `set search_path = public, pg_temp`, and deliberate `authenticated` execution. No browser path receives a service-role key or a provider credential.

| Disposition | Functions / family | Actual caller and scope | Why SECURITY DEFINER remains | Owner action |
|---|---|---|---|---|
| KEEP — governed browser | `create_job_assignment_with_asset_guard`, `update_job_assignment_with_event`, `transition_job_assignment_with_event`, `cancel_job_assignment_with_event`, `publish_shift_with_event`, `cancel_shift_with_event`, `update_shift_with_asset_guard`, `save_job_stops` | Manager job planner; actor company and target assignment/shift ownership | Atomic lifecycle/event writes and readiness triggers must run as the owner | Retain fixed path, auth/RBAC checks, and audit |
| KEEP — governed browser | `begin_job_evidence_upload`, `finalize_job_evidence_upload`, `cleanup_failed_job_evidence_upload`, `assign_trailer_to_job_assignment`, `set_operational_task_handling`, `sync_atlas_signal_observations` | Driver/manager operational UI; assignment or source ownership is checked server-side | Upload intent, trailer readiness, handling concurrency, and Atlas observation upserts cross RLS boundaries | Retain narrow grants and bounded inputs |
| KEEP — governed browser | `review_job_evidence_governed`, `list_manager_pod_review_queue`, `list_manager_operational_timeline`, `list_pod_reconciliation_drilldown` | Manager UI; `auth.uid()`, same-company manager check, same-company job/assignment joins | Safe review outcomes, protected evidence metadata, and bounded cross-table projections | Batch 18 contract; no raw storage internals in queue/timeline |
| KEEP — governed browser | `create_atlas_proposal`, `list_atlas_proposals`, `review_atlas_proposal`, `revalidate_atlas_proposal`, `atlas_apply_shift_vehicle_proposal`, `atlas_apply_trailer_proposal`, `atlas_apply_task_proposal`, `get_atlas_proposal_timeline` | Manager Atlas workbench; company, target ownership, proposal state and permission checks | Proposal validation/application is an audited multi-table transaction | Retain; no arbitrary Atlas SQL/RPC execution |
| KEEP — governed browser | `get_company_tacho_signals`, `get_driver_tacho_analysis_bundle`, `get_driver_timeline_bundle`, `get_import_timeline_bundle`, `get_tacho_import_bundle`, `get_vehicle_timeline_bundle`, `get_vehicle_unit_analysis_bundle`, `get_tacho_processing_runtime` | Manager/tacho screens; company and requested driver/vehicle/import ownership | Private tachograph stores are intentionally not browser-readable directly | Retain private helper boundary and fixed path |
| KEEP — governed browser | `acknowledge_tachograph_finding_review`, `assign_tachograph_training`, `archive_tacho_candidate_import`, `confirm_tacho_candidate_import_storage_deleted`, `mark_tacho_candidate_card_review`, `pair_tacho_card_import_to_driver`, `patch_tachograph_import_metadata`, `prepare_tacho_import_reprocess`, `save_tachograph_finding_review` | Manager tacho review workflows; current RBAC and company/target checks | Review and reprocessing state transitions require private writes and audit | Retain; review high-risk write paths in the next tacho-specific audit |
| KEEP — internal | `record_security_event`, `record_shift_audit_event`, `prevent_role_escalation`, `prevent_stale_driver_compliance_resolution`, `prevent_vehicle_return_to_service_with_open_defects`, `enforce_asset_assignment_readiness`, `enforce_trailer_assignment_readiness`, `set_*_updated_at`, `audit_atlas_proposal_validation_transition`, `dispatch_*` private trigger helpers | Triggers, cron, or server maintenance; no browser caller | Trigger/maintenance execution must remain independent of end-user table grants | Keep internal grants only; do not add browser policies |
| KEEP — internal | `actor_can_access_driver`, `actor_can_access_vehicle`, `actor_can_export`, `actor_has_permission`, `current_actor_company_id`, `current_actor_legacy_role`, `get_my_company_id`, `get_my_role`, `get_auth_user_company`, `get_auth_user_role`, `is_manager_for_company_path`, `is_uuid`, `map_tachograph_status_to_contract`, `resolve_tacho_range_start`, `daily_timeline_summary_json`, `timeline_event_json`, `timeline_gap_json`, `timeline_generation_json` | RLS/function helpers and typed serializers | These are policy/serialization primitives, not direct data endpoints | Keep fixed path; no broad execute grants |
| CHANGE — revoke / already revoked | Legacy `create_job_evidence`, `create_job_assignment_with_event`, `record_atlas_proposal_outcome`; superseded UUID invite overloads | No current Portal caller; current upload, assignment, and proposal clients use governed replacements | Direct or obsolete write paths are not part of the active browser contract | Existing migrations already revoke their public/anonymous or authenticated execution; do not replay old migrations |
| DEFER — compatibility review | Legacy `review_job_evidence` | Historical smoke/mobile compatibility is not proven absent; current Portal wrapper now calls `review_job_evidence_governed` | Removing the old grant without an external-client inventory could break an untracked client | Revoke only after owner confirms mobile/external callers are migrated; no new UI uses it |
| DEFER — internal ownership | `configure_tacho_processing_runtime`, `prune_pod_reconciliation_runs`, `purge_company_driver_card_reads`, `reconcile_expired_job_evidence_uploads`, `rotate_company_auth_code`, `update_company_name`, `update_driver_profile`, `update_incident_follow_up`, `update_vehicle_check_lifecycle`, `upsert_company_operator_licence_profile` | Mixed admin, cron, and existing Portal callers; caller inventory needs a dedicated owner check | Some are maintenance or privileged profile operations, but caller migration evidence is incomplete | No speculative invoker conversion in Batch 18 |
| REMOVE — obsolete only after proof | Old `accept_driver_invite` / `lookup_pending_driver_invite` compatibility path and any superseded tacho reset overloads | Legacy onboarding/reset clients are not represented in this Portal | Removal is safe only after the owner confirms no deployed client depends on the overload | Keep revoked/compatibility state and schedule deletion in a controlled cleanup |

## RLS-without-policy disposition

The 16 policyless RLS tables remain classified as internal/service-ledger or tachograph processing stores. They have no browser table grants and are not exposed through the Batch 18 POD queue, timeline, or Atlas gateway. Their disposition is `DEFER — owner confirms service runtime`, not “add a permissive policy”. The two new Atlas tables are also private-schema, RLS-enabled, and revoked from `public`, `anon`, and `authenticated`; they are server-only future contracts.

## Batch 18 conclusion

The only new browser-facing SECURITY DEFINER functions are the governed POD review and queue functions. They use fixed search paths, same-company joins, manager checks, bounded notes, row locking, conditional freshness, deliberate authenticated grants, and audit events. No unrelated legacy function was converted to invoker mode or deleted speculatively. Linked database lint after deployment reported no warning for the Batch 18 functions; remaining lint errors are pre-existing legacy-function issues recorded for owner follow-up.
